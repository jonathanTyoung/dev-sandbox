def function_name(arr):
    # 1. Handle edge cases
    if not arr or len(arr) == 0:
        return # appropriate value
    
    # 2. Initialize tracking variables
    result = 0  # or whatever you're counting/tracking
    
    # 3. Main loop/logic
    for i in range(len(arr)):
        # Do the work
        pass
    
    # 4. Return result
    return result